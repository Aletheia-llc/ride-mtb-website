'use client'

interface Step1UsernameProps {
  defaultValues: { username: string }
}

export default function Step1Username({ defaultValues: _defaultValues }: Step1UsernameProps) {
  return <div>Step 1 placeholder</div>
}
