'use client'

interface Step3AboutYouProps {
  defaultValues: { bio: string; location: string }
}

export default function Step3AboutYou({ defaultValues: _defaultValues }: Step3AboutYouProps) {
  return <div>Step 3 placeholder</div>
}
