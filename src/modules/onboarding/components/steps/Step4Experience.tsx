'use client'

interface Step4ExperienceProps {
  defaultValues: { yearsRiding: number | null; favoriteBike: string; favoriteTrail: string }
}

export default function Step4Experience({ defaultValues: _defaultValues }: Step4ExperienceProps) {
  return <div>Step 4 placeholder</div>
}
