'use client'

interface Step5InterestsProps {
  defaultValues: { interests: string[] }
}

export default function Step5Interests({ defaultValues: _defaultValues }: Step5InterestsProps) {
  return <div>Step 5 placeholder</div>
}
