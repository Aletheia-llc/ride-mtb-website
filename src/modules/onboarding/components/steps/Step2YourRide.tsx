'use client'

interface Step2YourRideProps {
  defaultValues: { ridingStyle: string | null; skillLevel: string | null }
}

export default function Step2YourRide({ defaultValues: _defaultValues }: Step2YourRideProps) {
  return <div>Step 2 placeholder</div>
}
